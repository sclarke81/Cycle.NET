<?xml version="1.0" encoding="UTF-8" ?>
<ui:ContentPageBase x:Class="Cycle.NET.ToggleDemo.Views.ToggleDemoView"
                    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:rxui="clr-namespace:ReactiveUI.Maui;assembly=ReactiveUI.Maui"
                    xmlns:ui="clr-namespace:Cycle.NET.ToggleDemo.Views"
                    xmlns:vms="clr-namespace:Cycle.NET.ToggleDemo.ViewModels"
                    x:TypeArguments="vms:ToggleDemoViewModel"
                    x:DataType="vms:ToggleDemoViewModel">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <CheckBox
            x:Name="Toggled" />
        <Label
            Grid.Column="1"
            Text="Toggle me"
            VerticalTextAlignment="Center" />
        <Label
            x:Name="Status"
            Grid.Row="1"
            Grid.ColumnSpan="2" />
    </Grid>
</ui:ContentPageBase>